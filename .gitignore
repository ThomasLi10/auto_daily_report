# local secrets — NEVER commit (CalDAV password / webhook signing secret)
.feishu_caldav.json
.feishu_webhook.json

# local site config — NEVER commit (extra service accounts, OUT_DIR override)
daily_report.local

# python
__pycache__/
*.pyc

# editor / os
.DS_Store
